 <div class="wallDP">
 <%if @user.pic_file_name? %>
        <%= link_to (image_tag @user.pic.url(:wall)), @user.pic.url %>
        <%else%>
          <%=image_tag("pic_medium_m.png", alt: "Rails")%>
        <%end%>
 </div>
  <div class="pull-left blueText">
    <b><%= link_to "#{current_user.fName} #{current_user.lName}", user_path(@user) %><br></b>
    <%= @user.email%><br>
 </div>